# Copy to .env on Byte-Me. Never commit the real file.
ADMIN_TOKEN=replace-with-a-long-random-admin-token
# Private host folder where RaceTec atomically replaces its one RDF export.
RACE_EXPORT_DIR=/srv/racetec-export
RACE_EXPORT_FILENAME=results.rdf

